{
  "tasks": [
    {
      "id": "dev",
      "label": "dev server",
      "description": "Start the development server",
      "group": "serve",
      "default": true,
      "watch": false,
      "cmd": ["npm", "run", "dev"],
      "cwd": ".",
      "env": {
        "NODE_ENV": "development"
      }
    },
    {
      "id": "test",
      "cmd": ["zig", "build", "test"]
    }
  ]
}
